# Scenario

**Feature**: `--here` + create+`--bring` quiet contract variants

```
wrk src --no-config --here --bring …
  -> create path only; no will bring: / SKIP / external path lines
  -> -v ignored
```

## Steps

- Leaves set fixtures + `--here` (and optional `-v` / soft-skip shape).
