# Scenario

**Feature**: unwind validation and help expose the allowed composition surface

```
CLI arguments -> unwind validation/help -> allowed partners or retained error
```

```go
import "github.com/xhd2015/doctest/session"
func Setup(t *testing.T,d *session.Doctest,req *Request) error { _=d; seedMain(t,req); return nil }
```
